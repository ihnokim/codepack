[conn]
path = config/conn.ini

[scheduler]
source = mongodb
db = codepack
collection = scheduler

[code_storage]
source = mongodb
db = codepack
collection = codes

[codepack_storage]
source = mongodb
db = codepack
collection = codepacks

[cache]
source = mongodb
db = codepack
collection = cache

[code_snapshot]
source = mongodb
db = codepack
collection = state
