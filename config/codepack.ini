[conn]
filepath = config/conn.ini

[scheduler]
source = mongodb
db = codepack
collection = scheduler

[code]
source = mongodb
db = codepack
collection = codes

[codepack]
source = mongodb
db = codepack
collection = codepacks

[cache]
source = mongodb
db = codepack
collection = cache

[state]
source = mongodb
db = codepack
collection = state
